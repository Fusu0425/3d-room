# 个人 3D 房间实施方案

## 项目目标

制作一个与参考视频同类视觉语言的个人 3D 房间，并同时支持：

- 自由旋转、缩放和点击探索；
- 书籍、音乐、照片、旅行和个人文字的本地编辑；
- 后续加入游戏剧场与自动导演镜头；
- 最终发布为可访问链接，并录制精美展示视频。

当前阶段只制作个人房间，不建设面向其他用户的 DIY 平台。

## 锁定的空间方案

最终构图以 `design/room-layout-final.png` 为准。

- 中央：白色书桌、显示器、键盘、鼠标、桌灯、相机、水杯、办公椅和地毯。
- 左墙：白板和旅行照片墙。
- 左侧地面：行李箱、赵一鸣零食袋、唱片机、唱片柜和唱片。
- 右墙：并排的《艾尔登法环》和《光与影：33号远征队》海报。
- 右侧地面：矮书架、十本书、正常结构的跑步机、跑鞋和羽毛球。
- 右上墙：两支羽毛球拍。

正式建模时保留整体构图，但不复制概念图中不准确的物体数量、文字和透视误差。

## 正式制作时的视觉修正

1. 跑步机保持完整控制台、双扶手、安全钥匙、立柱、履带和前电机仓，整体尺寸比概念图缩小约 8% 至 12%。
2. 书架严格只展示十本主书；每本书拥有独立网格、书脊、封面和点击区域。
3. 零食袋缩小约 15%，颜色略微降饱和，半藏在行李箱与唱片柜之间，避免抢走游戏海报的注意力。
4. 两张游戏海报使用真实封面资源，但模型框架、玻璃反射和照明统一。
5. 默认相机使用适中的透视，不允许右墙或跑步机遮挡主体；轨道控制限制在经过测试的安全角度。
6. 白天光线保留左侧窗影；另建可切换的夜间灯光，为音乐和后续游戏演出提供基础。
7. 所有家具接地并具有统一比例、接触阴影和材质粗糙度，禁止悬空与穿模。

## 技术架构

- 前端：React、Vite、Three.js、React Three Fiber、Drei。
- 3D 资源：glTF/GLB；简单结构由代码参数化生成，复杂物件使用独立模型。
- 状态：房间数据与 3D 场景分离；物品通过稳定 ID 关联内容和交互。
- 本地编辑：IndexedDB 保存图片、音频和内容数据；文本设置可同步保存在 JSON。
- 备份迁移：支持导出与导入 Room Package，包含配置与用户资源。
- 发布模式：公开页面只读；本地编辑入口不出现在普通访问界面。
- 后续导演模式：相机镜头、灯光、声音和物品触发使用独立时间线配置。

## 数据结构原则

每个交互物品至少包含：

```text
id
type
transform（位置、旋转、缩放）
cameraFocus（点击后的相机位置与观察点）
hoverLabel
content
media
lightingCue
enabled
```

书籍、音乐、照片等内容不得直接写死在模型组件中。

## 镜头交互规范

- 访客悬停物品时只出现克制的轮廓、亮度变化和名称，不弹出内容。
- 点击物品后暂时锁定手动旋转，相机在约 0.9 至 1.3 秒内平滑移动到该物品专属的正面机位。
- 每件物品独立配置相机位置、观察目标和安全距离，不使用同一套机械放大逻辑。
- 相机到位后再显示内容，房间继续保留在背景中，只做轻微压暗或景深处理。
- 关闭内容后，相机按原路径返回访客点击前的精确位置，而不是强制回到默认镜头。
- `Esc`、关闭按钮和点击允许的背景区域均可退出；相机运动期间禁止重复触发其他物品。
- 提供固定的“重置视角”入口，随时回到最终参考图对应的默认构图。
- 桌面小物使用略大的不可见命中区域，视觉模型不被迫放大，也能容易点击。

## 开发阶段与验收标准

### 阶段 1：项目基础

- 建立可启动、构建和预览的前端项目；
- 建立房间配置、物品注册和内容数据结构；
- 提供加载画面、错误边界和基础响应式容器。

验收：页面可以稳定打开，刷新后无控制台错误。

### 阶段 2：完整空间灰模

- 按最终参考图建立房间、墙体、地板、地毯和全部物品占位；
- 确定默认镜头、旋转范围、缩放范围和重置视角；
- 桌椅、跑步机和左右功能区比例正确，无相互遮挡。

验收：即使全部使用灰色材质，也能一眼识别布局和主要物体。

### 阶段 3：交互骨架

- 统一悬停反馈、光标、物品名称和点击命中区；
- 点击后镜头平滑聚焦，关闭后回到此前视角；
- 完成内容面板、键盘关闭、背景关闭和移动端基本交互；
- 先以摄影模块贯通“点击—上传—预览—保存—重载”。

验收：导入一张本地照片后，刷新页面仍能看到该照片。

### 阶段 4：正式模型与材质

- 依次完成桌面、音乐旅行区、书架、运动区、墙面内容和生活装饰；
- 每完成一个区域即在最终镜头下验收比例、细节和性能；
- 不使用纯长方体代替自行辨识度要求较高的物品。

验收：默认视角的视觉完成度接近锁定概念图，且拖动镜头后不存在明显穿模。

### 阶段 5：个人内容模块

- 十本书：封面、作者、选择原因、读后感与思考；
- 音乐：本地音频、封面、播放列表和持续播放器；
- 摄影旅行：相册、照片墙、地点和旅程目录；
- 电脑：项目、代码片段和可交互终端；
- 运动：跑步与羽毛球文字、照片和记录；
- 白板：欢迎语与个人标志。

验收：全部内容可以通过编辑模式修改，不需要改源码。

### 阶段 6：视觉与性能精修

- 完成灯光、阴影、纹理、玻璃、环境反射和后期效果；
- 压缩模型与纹理，按需加载音频和相册；
- 在主流桌面尺寸及手机上测试；
- 建立性能降级策略，低性能设备减少阴影与后期。

验收：目标桌面设备稳定运行，初始画面与交互无明显卡顿。

### 阶段 7：游戏剧场与导演模式

- 为两款游戏分别制作多个短视觉实验；
- 选定房间变换、投影、声音和粒子方向；
- 编排自动镜头与最终视频时间线。

此阶段必须在基础房间验收后开始。

## 用户内容准备清单

开发基础房间时无需立即提供全部资料。之后依次需要：

1. 十本书的书名、作者和封面；
2. 初始歌曲列表及可以用于网页的音频文件；
3. 第一批旅行与生活照片；
4. 希望展示的项目与代码片段；
5. 跑步和羽毛球的文字、照片或数据；
6. 白板欢迎语与个人标志的最终文字；
7. 两款游戏的正式海报图片和后续演出素材。

## 当前优先级

项目脚手架、数据结构、完整空间灰模和基础镜头交互已经完成。`V1.5` 完成前五本书的全尺寸阅读器、编辑、自动保存和 JSON 备份恢复；`V1.6` 完成基于 IndexedDB 的本地照片相册；`V1.7` 完成本地音乐曲库、连续播放和场景唱片联动；`V1.8` 完成电脑端的项目档案、代码阅读和本地编辑工作台；`V1.9` 完成可编辑的房间序章与第一轮性能优化；`V2.0` 完成跑步与羽毛球的独立运动档案、文字编辑和本地照片集；`V2.1` 完成默认构图、光影、材质、投射阴影、生活细节、微交互、提示音与首次访问序章；`V2.2` 替换两张正式游戏海报并建立统一游戏剧场；`V2.3` 修复系统减少动态效果导致的演出黑屏问题，并完成《艾尔登法环》黑金预演和《33号远征队》绘画化预演。下一步统一房间音乐与游戏演出的淡入淡出，并开始导演镜头路线。

## 正式资产质量路线

灰模确认之后，不再通过反复修饰基础长方体来追求最终效果。正式画面使用三类资产：

### A. 必须准确识别的二维内容

- 十本书的原版封面、书脊与必要的背封信息；
- 《艾尔登法环》和《光与影：33号远征队》的正式宣传海报；
- 唱片或歌曲封面；
- 用户自己的照片与旅行图片；
- 白板标志和文字。

这类内容使用高分辨率原图制作纹理，不由生成模型虚构。书籍需要确定具体版本或 ISBN，避免使用错误版本的封面。

### B. 必须接近真实物理世界的三维物品

- 办公椅、键盘、鼠标、相机；
- 唱片机、唱片柜和黑胶唱片；
- 行李箱与零食袋；
- 跑步机、跑鞋、羽毛球拍和羽毛球。

这类物品使用正式 GLB 网格与 PBR 材质。需要真实轮廓、倒角、连接结构、透明件、金属件和合理尺寸，不允许使用不可辨认的方块占位进入最终版本。

### C. 可以参数化但仍需精细的空间结构

- 墙面、地板、地毯；
- 书桌、矮书架、画框和白板；
- 简单层板、柜体和灯光结构。

这类物品可以由代码生成，但必须拥有准确厚度、倒角、接缝、木纹或织物材质和接触阴影。

## 核心物品的完成标准

### 十本书

- 每本书是独立模型和独立点击目标；
- 使用用户确认的具体版本封面，包含正面、书脊和必要的背面；
- 在默认镜头中能通过书脊颜色和文字区分，在聚焦镜头中能清楚阅读书名；
- 十本书拥有符合真实书籍的不同高度、厚度和页数；
- 点击后书本轻微抽出，相机移动到正面，再进入读后感内容；
- 首批已确定候选：《思考，快与慢》《纳瓦尔宝典》《非暴力沟通》。

### 两张游戏海报

- 使用用户确认的正式宣传图，而不是生成的近似图；
- 画框具有真实厚度、玻璃反射和背板；
- 默认镜头中标题可辨认，聚焦后画面保持清晰；
- 为后续游戏剧场预留独立材质、灯光和转场控制。

当前状态：`V0.7` 已完成画框、玻璃与高清构图样板；官方宣传图替换保留为独立纹理步骤，不需要重建模型。

### 唱片机

- 具备机身、防尘盖、转盘、唱片、中心轴、唱臂、配重、唱头、控制按钮和铰链；
- 防尘盖使用真实透明材质，唱片具有细微沟槽和反射；
- 播放时唱片匀速旋转，唱臂落到唱片上；暂停或切歌时动画正确；
- 音乐从用户点击后开始，关闭内容面板后继续播放；
- 近景中不得出现锯齿、低面数折角或颗粒状模糊。

## 视觉制作与验收顺序

### Gate 1：渲染基线

- 白色、清爽、低对比背景；
- 无环境雾造成的灰蒙；
- 柔和日光、接触阴影和正确色彩空间；
- 白板与海报文字不得越界。

通过状态：已完成 `V0.2`。

更新状态：`V1.0` 在保留清爽白色方向的基础上重新统一环境反射、暗部层次和阴影强度，替代 `V0.2` 作为正式全局光照基线。

### Gate 2：英雄资产样板

先完成两组可以代表最终质量的物品：

1. 中央桌面：桌子、显示器、键盘、鼠标、台灯、相机、水杯和办公椅；
2. 左侧音乐区：唱片柜、正式唱片机与唱片。

这两组通过近景验收后，其材质密度、倒角大小、纹理清晰度和阴影标准成为全房间规范。唱片机结构样板已完成 `V0.4` 第一轮；中央桌面已完成 `V0.6` 第二轮，键盘、台灯与桌体结构通过近景检查，办公椅仍会在后续正式模型阶段继续升级。

### Gate 3：可识别内容样板

- 使用三本已确定书籍制作真实封面与书脊样板；
- 使用两张确认后的游戏正式宣传图制作海报；
- 验证默认镜头可辨认、近景可阅读、点击不会选错。

当前状态：`V1.2` 已确认并录入前五本书的具体出版版本、译者与 ISBN，完成对应封面、资料面板、阅读/编辑双模式、独立笔记、自动保存和刷新恢复验证。剩余五本继续保留独立实体与规范化占位。

完整十本书名单未确认前，剩余七本保留规范化占位，但尺寸和数据结构一次到位。

### Gate 4：其余正式资产

依次完成摄影旅行区、运动区、书架其余书籍、生活装饰。每个区域都必须通过默认视角、聚焦视角、侧后视角和移动端四类检查。

### Gate 5：内容编辑系统

在最终模型标准稳定后，接入照片、音乐、书籍文字和项目内容编辑、IndexedDB 保存与 Room Package 导入导出。

### Gate 6：全场景光照与性能

- 桌面目标为主流设备稳定 60 FPS；
- 使用 Meshopt/Draco 压缩网格、KTX2 压缩纹理；
- 纹理按可见距离分级，远处不加载不必要的 2K/4K 资源；
- 使用 LOD、延迟加载和自适应 DPR；
- 清晰度不足时优先修正模型、纹理和过滤，不使用强景深或模糊掩盖问题。

### Gate 7：游戏剧场与导演模式

基础房间和所有普通交互通过验收后，再制作两款游戏的视觉实验、房间变换、音乐演出、自动镜头和最终视频。

## 工作方式

项目采用“完整路线 + 小步验收”，而不是两种极端方式：

- 不会一次性制作所有资产，最后才发现整体方向错误；
- 也不会没有标准地无限微调同一个方块模型。

每一个 Gate 都有明确输入、成品和验收标准。通过后原则上不再推翻，只在后续全局检查中做必要修正。



